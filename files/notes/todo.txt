
- auto-complete in precedence table (reduceOnEror happens, but the result is still not valid for PsiToGrammarConverter)
- code formatter
- auto-close comments is broken when typing /* enter
- generate using meta-grammar file and make sure the output is identical to that of the bootstrap grammar
- TODO markers in code
